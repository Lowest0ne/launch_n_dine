<nav>
  <ul>
  <% if user_signed_in? %>
    <li><%= link_to 'Sign Out' %></li>
  <% else %>
    <li><%= link_to 'Restaurant Owner?', new_user_registration_path( role: 'owner' ) %></li>
  <% end %>
  </ul>
</nav>
