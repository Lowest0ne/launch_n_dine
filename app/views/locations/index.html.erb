
<ul>
  <% @locations.each do | location | %>
  <li>
    <%= link_to "#{location.street} #{location.city}, #{location.state}",
                edit_location_path( location )%>
    <%= link_to "Delete",  location_path( location ), method: :delete %>
  </li>
  <% end %>
</ul>
