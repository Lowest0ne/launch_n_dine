<%= simple_form_for @menu do |form| %>
  <%= form.input :name,
        label: false,
        placeholder: 'Menu Name',
        required: true,
        autofocus: true
  %>
  <%= form.button :submit %>
<% end %>
