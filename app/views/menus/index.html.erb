<%= content_tag :p, "Menus for #{@restaurant.name}" %>
<%= render @menus %>

<% if current_user && @restaurant.user == current_user %>
  <%= link_to 'Add Menu', new_restaurant_menu_path( @restaurant ) %>
<% end %>
