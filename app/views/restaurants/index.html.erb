<%= content_tag :h3, 'Our Restaurants' %>
<%= render @restaurants %>

<% if current_user && current_user.is_owner? %>
  <%= link_to 'Add Restaurant', new_user_restaurant_path( current_user ) %>
<% end %>
